version: "3.8"

services:
  next:
    build: .
    container_name: next-app
    environment:
      NODE_ENV: production
    ports:
      - "3001:3001"
    restart: always

networks:
  web_network:
    external: true
